---
phase: 01-agent-structure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/agents/ta_ensemble/__init__.py
  - src/backend/agents/ta_ensemble/weekly_subagent.py
  - src/backend/agents/ta_ensemble/daily_subagent.py
  - src/backend/agents/ta_ensemble/fourhour_subagent.py
  - src/backend/agents/ta_ensemble/ta_mentor.py
  - src/backend/agents/nansen_agent.py
  - src/backend/agents/telegram_agent.py
  - src/backend/agents/risk_agent.py
  - src/backend/agents/orchestrator.py
  - src/backend/agents/__init__.py
autonomous: true
requirements:
  - AGNT-01
  - AGNT-02
  - AGNT-03
  - AGNT-04
  - AGNT-05

must_haves:
  truths:
    - "ta_ensemble/ folder exists with 4 subagent files and __init__.py"
    - "Root-level agent files follow naming convention (*_agent.py)"
    - "All agent files can be imported without errors"
    - "Orchestrator references updated to use new structure"
  artifacts:
    - path: "src/backend/agents/ta_ensemble/__init__.py"
      provides: "Package exports for TA subagents"
      exports: ["WeeklySubagent", "DailySubagent", "FourHourSubagent", "TAMentor"]
    - path: "src/backend/agents/ta_ensemble/weekly_subagent.py"
      provides: "Weekly timeframe TA analysis stub"
      contains: "class WeeklySubagent"
    - path: "src/backend/agents/ta_ensemble/daily_subagent.py"
      provides: "Daily timeframe TA analysis stub"
      contains: "class DailySubagent"
    - path: "src/backend/agents/ta_ensemble/fourhour_subagent.py"
      provides: "4-hour timeframe TA analysis stub"
      contains: "class FourHourSubagent"
    - path: "src/backend/agents/ta_ensemble/ta_mentor.py"
      provides: "TA Mentor that synthesizes subagent outputs"
      contains: "class TAMentor"
    - path: "src/backend/agents/nansen_agent.py"
      provides: "Nansen on-chain analysis agent"
      contains: "class NansenAgent"
    - path: "src/backend/agents/telegram_agent.py"
      provides: "Telegram signal scanning agent"
      contains: "class TelegramAgent"
    - path: "src/backend/agents/risk_agent.py"
      provides: "Risk management agent"
      contains: "class RiskAgent"
    - path: "src/backend/agents/orchestrator.py"
      provides: "Main orchestrator coordinating all agents"
      contains: "class Orchestrator"
  key_links:
    - from: "src/backend/agents/__init__.py"
      to: "all agent modules"
      via: "imports and __all__ exports"
      pattern: "from.*import"
    - from: "src/backend/agents/ta_ensemble/__init__.py"
      to: "subagent modules"
      via: "relative imports"
      pattern: "from.*subagent import"
---

<objective>
Create the nested ta_ensemble/ folder structure with timeframe-specific subagents and refactor root-level agent files to match the architectural naming convention.

Purpose: Establish clean agent folder hierarchy that separates TA concerns by timeframe and provides a clear organizational structure for future agent implementations.

Output: Complete agent folder structure with importable stub files that pass Python import verification.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Existing code reference (use as logic reference, do NOT copy directly):
- src/backend/agents/ta_ensemble.py (current monolithic TA agent)
- src/backend/agents/mentor.py (current mentor agent)
- src/backend/agents/base.py (base agent class to inherit from)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create nested ta_ensemble/ folder with subagent stubs</name>
  <files>
    src/backend/agents/ta_ensemble/__init__.py
    src/backend/agents/ta_ensemble/weekly_subagent.py
    src/backend/agents/ta_ensemble/daily_subagent.py
    src/backend/agents/ta_ensemble/fourhour_subagent.py
    src/backend/agents/ta_ensemble/ta_mentor.py
  </files>
  <action>
    1. Create the ta_ensemble/ directory inside src/backend/agents/

    2. Create weekly_subagent.py with:
       - Class WeeklySubagent inheriting from BaseAgent (from ..base import BaseAgent)
       - Stub analyze(symbol: str, context: dict) -> dict method returning placeholder TASignal-compatible dict
       - Docstring indicating this handles weekly timeframe analysis
       - System prompt placeholder for weekly TA focus

    3. Create daily_subagent.py with:
       - Class DailySubagent inheriting from BaseAgent
       - Same stub structure as weekly but for daily timeframe

    4. Create fourhour_subagent.py with:
       - Class FourHourSubagent inheriting from BaseAgent
       - Same stub structure for 4-hour timeframe

    5. Create ta_mentor.py with:
       - Class TAMentor inheriting from BaseAgent
       - Stub synthesize(weekly: dict, daily: dict, fourhour: dict) -> dict method
       - This agent synthesizes the 3 timeframe subagent outputs into TAMentorSignal
       - Reference existing mentor.py logic as pattern (conflict detection, confidence adjustment)

    6. Create __init__.py with:
       - Imports from all 4 submodules
       - __all__ = ['WeeklySubagent', 'DailySubagent', 'FourHourSubagent', 'TAMentor']

    Note: Each stub should return a minimal valid dict structure that can later be replaced with Pydantic models in Phase 2. Include TODO comments marking where Pydantic integration will happen.
  </action>
  <verify>
    python -c "from src.backend.agents.ta_ensemble import WeeklySubagent, DailySubagent, FourHourSubagent, TAMentor; print('ta_ensemble imports OK')"
  </verify>
  <done>
    ta_ensemble/ folder exists with 5 files (__init__.py + 4 subagent files), all classes importable without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Refactor root-level agents to match naming convention</name>
  <files>
    src/backend/agents/nansen_agent.py
    src/backend/agents/telegram_agent.py
    src/backend/agents/risk_agent.py
    src/backend/agents/orchestrator.py
    src/backend/agents/__init__.py
  </files>
  <action>
    1. Create nansen_agent.py:
       - Move/refactor content from existing nansen.py
       - Keep class name as NansenAgent
       - Ensure analyze() stub returns minimal valid dict
       - Add TODO comment for Pydantic NansenSignal integration

    2. Create telegram_agent.py:
       - Move/refactor content from existing telegram.py
       - Keep class name as TelegramAgent
       - Ensure analyze() stub returns minimal valid dict
       - Add TODO comment for Pydantic TelegramSignal integration

    3. Create risk_agent.py:
       - Move/refactor content from existing risk_levels.py
       - Rename class to RiskAgent (from RiskLevelsAgent)
       - Ensure analyze() stub returns minimal valid dict
       - Add TODO comment for Pydantic RiskOutput integration

    4. Update orchestrator.py:
       - Update imports to use new ta_ensemble package (from .ta_ensemble import TAMentor, WeeklySubagent, etc.)
       - Update imports for renamed agents (from .risk_agent import RiskAgent)
       - Keep existing orchestration logic as reference but simplify to stub that returns valid dict
       - Add TODO comment for Pydantic OrchestratorOutput integration

    5. Update __init__.py:
       - Update imports to reflect new file names
       - Update __all__ to export: BaseAgent, WeeklySubagent, DailySubagent, FourHourSubagent, TAMentor, NansenAgent, TelegramAgent, RiskAgent, Orchestrator
       - Remove old file references (nansen, telegram, risk_levels, ta_ensemble as file)

    6. Clean up old files (optional - leave for now, mark as deprecated):
       - Add deprecation comment to nansen.py, telegram.py, risk_levels.py, ta_ensemble.py, mentor.py
       - OR delete them if confident the new structure is correct

    Decision: Keep old files with deprecation comments for safe rollback during v0.1. Delete in v1.0.
  </action>
  <verify>
    python -c "from src.backend.agents import NansenAgent, TelegramAgent, RiskAgent, Orchestrator, TAMentor; print('Root agent imports OK')"
  </verify>
  <done>
    All root-level agents follow *_agent.py naming convention, __init__.py exports all agents correctly, imports work without errors
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

1. Full import test:
   ```bash
   python -c "
   from src.backend.agents import (
       WeeklySubagent, DailySubagent, FourHourSubagent, TAMentor,
       NansenAgent, TelegramAgent, RiskAgent, Orchestrator
   )
   print('All agents importable')

   # Verify classes exist and have analyze methods
   for cls in [WeeklySubagent, DailySubagent, FourHourSubagent, NansenAgent, TelegramAgent, RiskAgent]:
       assert hasattr(cls, 'analyze'), f'{cls.__name__} missing analyze method'
   assert hasattr(TAMentor, 'synthesize'), 'TAMentor missing synthesize method'
   print('All stub methods present')
   "
   ```

2. Folder structure verification:
   ```bash
   ls -la src/backend/agents/ta_ensemble/
   # Should show: __init__.py, weekly_subagent.py, daily_subagent.py, fourhour_subagent.py, ta_mentor.py

   ls src/backend/agents/*_agent.py
   # Should show: nansen_agent.py, telegram_agent.py, risk_agent.py
   ```
</verification>

<success_criteria>
- ta_ensemble/ folder exists with weekly_subagent.py, daily_subagent.py, fourhour_subagent.py, ta_mentor.py
- nansen_agent.py, telegram_agent.py, risk_agent.py, orchestrator.py exist at src/backend/agents/ root
- All agent files contain stub functions that can be imported without errors
- Running `python -c "from src.backend.agents import *"` succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/01-agent-structure/01-01-SUMMARY.md`
</output>
