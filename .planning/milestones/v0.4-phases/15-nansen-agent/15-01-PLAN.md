---
phase: 15-nansen-agent
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/agents/nansen_mcp.py
  - src/backend/agents/__init__.py
autonomous: true
requirements: [NANS-01, NANS-02, NANS-03, NANS-04, NANS-05, NANS-06]

must_haves:
  truths:
    - "Exchange flow data can be fetched via MCP and returns inflow/outflow direction"
    - "Smart money activity can be fetched via MCP and returns accumulating/distributing direction"
    - "Whale activity can be fetched via MCP and returns accumulating/distributing direction"
    - "Top PnL wallets can be fetched via MCP and returns bullish/bearish bias"
    - "Fresh wallet activity is handled gracefully (neutral if no tool available)"
    - "Funding rate can be fetched from Hyperliquid perps endpoint"
  artifacts:
    - path: "src/backend/agents/nansen_mcp.py"
      provides: "MCP integration functions for all 5 Nansen signals + funding rate"
      exports: ["fetch_exchange_flows", "fetch_smart_money", "fetch_whale_activity", "fetch_top_pnl", "fetch_fresh_wallets", "fetch_funding_rate"]
  key_links:
    - from: "src/backend/agents/nansen_mcp.py"
      to: "Nansen MCP tools"
      via: "MCP tool invocation"
      pattern: "mcp__nansen__"
---

<objective>
Create MCP integration layer for fetching all 5 Nansen on-chain signals plus funding rate data.

Purpose: Provide clean Python functions that invoke Nansen MCP tools and parse responses into structured data suitable for signal aggregation.
Output: nansen_mcp.py module with 6 fetch functions covering all required on-chain data sources.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-nansen-agent/15-CONTEXT.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/backend/models/nansen_signal.py:
```python
class ExchangeFlows(BaseModel):
    net_direction: Literal["inflow", "outflow", "neutral"]
    magnitude: Literal["high", "medium", "low"]
    interpretation: str
    confidence: int  # 0-100

class SmartMoney(BaseModel):
    direction: Literal["accumulating", "distributing", "neutral"]
    confidence: int  # 0-100
    notable_wallets: list[str]
    interpretation: str

class WhaleActivity(BaseModel):
    summary: str
    notable_transactions: list[str]
    net_flow: Literal["accumulating", "distributing", "neutral"]
    confidence: int  # 0-100

class TopPnL(BaseModel):
    traders_bias: Literal["bullish", "bearish", "mixed", "neutral"]
    average_position: Literal["long", "short", "neutral"]
    confidence: int  # 0-100
    interpretation: str

class FreshWallets(BaseModel):
    activity_level: Literal["high", "medium", "low", "none"]
    trend: Literal["increasing", "decreasing", "stable"]
    notable_count: int
    interpretation: str

class FundingRate(BaseModel):
    rate: Optional[float]
    available: bool
    interpretation: str
```

Nansen MCP Tools Available:
- `mcp__nansen__token_flows` — holder_segment: "exchange" | "whale" | "smart_money"
- `mcp__nansen__smart_traders_and_funds_token_balances` — smart money holdings
- `mcp__nansen__token_current_top_holders` — top holders by segment
- `mcp__nansen__token_pnl_leaderboard` — trader PnL rankings
- `mcp__nansen__smart_traders_and_funds_perp_trades` — Hyperliquid perp trades (has funding context)
- `mcp__nansen__hyperliquid_leaderboard` — Hyperliquid trader leaderboard
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create nansen_mcp.py with exchange flow and whale activity fetchers</name>
  <files>src/backend/agents/nansen_mcp.py</files>
  <action>
Create src/backend/agents/nansen_mcp.py with:

1. Import logging, typing, and dataclasses for structured returns
2. Create dataclass `MCPSignalResult` with fields: success (bool), data (dict | None), error (str | None)

3. `fetch_exchange_flows(symbol: str, chain: str = "ethereum") -> MCPSignalResult`:
   - Use mcp__nansen__token_flows with holder_segment="exchange", dateRange={"from": "24H_AGO", "to": "NOW"}
   - Parse response to determine net_direction (inflow if inflows > outflows, outflow if outflows > inflows, else neutral)
   - Calculate magnitude based on USD flow volume (high > $10M, medium > $1M, low otherwise)
   - Return MCPSignalResult with data containing: net_direction, magnitude, interpretation, confidence
   - Handle errors gracefully — return MCPSignalResult(success=False, error=str(e))

4. `fetch_whale_activity(symbol: str, chain: str = "ethereum") -> MCPSignalResult`:
   - Use mcp__nansen__token_flows with holder_segment="whale", dateRange={"from": "24H_AGO", "to": "NOW"}
   - Parse response to determine net_flow (accumulating if inflows > outflows, distributing otherwise)
   - Use mcp__nansen__token_current_top_holders with label_type="whale" to get notable wallets
   - Return MCPSignalResult with data containing: summary, notable_transactions, net_flow, confidence

5. Add module docstring explaining this is the MCP integration layer for Nansen on-chain signals.

NOTE: The actual MCP tool invocation will happen via Claude's tool calling — these functions prepare the request parameters and will be called by the agent which has MCP access. Structure functions to return the MCP tool name and parameters needed, then process raw MCP response.
  </action>
  <verify>
    <automated>python -c "from src.backend.agents.nansen_mcp import fetch_exchange_flows, fetch_whale_activity, MCPSignalResult; print('Imports OK')"</automated>
  </verify>
  <done>fetch_exchange_flows and fetch_whale_activity functions exist and import successfully</done>
</task>

<task type="auto">
  <name>Task 2: Add smart money and top PnL fetchers</name>
  <files>src/backend/agents/nansen_mcp.py</files>
  <action>
Add to nansen_mcp.py:

1. `fetch_smart_money(symbol: str, chain: str = "ethereum") -> MCPSignalResult`:
   - Use mcp__nansen__token_flows with holder_segment="smart_money", dateRange={"from": "24H_AGO", "to": "NOW"}
   - Combine with mcp__nansen__smart_traders_and_funds_token_balances for balance change context
   - Determine direction: accumulating if net inflows and positive 24h balance change, distributing if net outflows, else neutral
   - Extract notable_wallets from balance data (top 5 addresses)
   - Return MCPSignalResult with data containing: direction, confidence, notable_wallets, interpretation

2. `fetch_top_pnl(symbol: str, chain: str = "ethereum") -> MCPSignalResult`:
   - Use mcp__nansen__token_pnl_leaderboard with dateRange={"from": "7D_AGO", "to": "NOW"}, order_by="pnlUsdTotal"
   - Analyze top 10 traders: count positive vs negative PnL
   - Determine traders_bias: bullish if >60% positive, bearish if <40% positive, mixed otherwise
   - Determine average_position: long if net positive, short if net negative, neutral if mixed
   - Return MCPSignalResult with data containing: traders_bias, average_position, confidence, interpretation

3. Handle MCP errors gracefully with logging.warning and return success=False
  </action>
  <verify>
    <automated>python -c "from src.backend.agents.nansen_mcp import fetch_smart_money, fetch_top_pnl; print('Imports OK')"</automated>
  </verify>
  <done>fetch_smart_money and fetch_top_pnl functions exist with proper MCP integration patterns</done>
</task>

<task type="auto">
  <name>Task 3: Add fresh wallets and funding rate fetchers, export from agents module</name>
  <files>src/backend/agents/nansen_mcp.py, src/backend/agents/__init__.py</files>
  <action>
1. Add to nansen_mcp.py:

`fetch_fresh_wallets(symbol: str, chain: str = "ethereum") -> MCPSignalResult`:
   - NOTE: No direct "fresh wallets" MCP tool exists
   - Per user decision: mark unavailable if no tool, return neutral with confidence 0
   - Try mcp__nansen__token_who_bought_sold to approximate new buyer activity
   - If data available: calculate activity_level based on volume, trend based on comparison periods
   - If no data: return MCPSignalResult with success=True but data containing activity_level="none", trend="stable", interpretation="Fresh wallet data not available via MCP", confidence=0

`fetch_funding_rate(symbol: str) -> MCPSignalResult`:
   - Use mcp__nansen__smart_traders_and_funds_perp_trades to check for Hyperliquid funding context
   - Alternatively use mcp__nansen__hyperliquid_leaderboard for top PnL perp traders
   - Extract funding rate if available in response data
   - Per PROJECT.md: Above +0.01% = crowded longs = bearish contrarian, Below -0.01% = crowded shorts = bullish contrarian
   - Return MCPSignalResult with data containing: rate, available, interpretation
   - If funding rate not extractable: return available=False, rate=None, interpretation="Funding rate not available from MCP tools"

2. Update src/backend/agents/__init__.py:
   - Add import: from .nansen_mcp import (fetch_exchange_flows, fetch_smart_money, fetch_whale_activity, fetch_top_pnl, fetch_fresh_wallets, fetch_funding_rate, MCPSignalResult)
   - Add to __all__: "fetch_exchange_flows", "fetch_smart_money", "fetch_whale_activity", "fetch_top_pnl", "fetch_fresh_wallets", "fetch_funding_rate", "MCPSignalResult"
  </action>
  <verify>
    <automated>python -c "from src.backend.agents import fetch_exchange_flows, fetch_funding_rate, MCPSignalResult; print('All exports OK')"</automated>
  </verify>
  <done>All 6 fetch functions exported from agents module, graceful degradation for unavailable data</done>
</task>

</tasks>

<verification>
1. All 6 fetch functions import successfully from src.backend.agents
2. MCPSignalResult dataclass exists with success, data, error fields
3. Each function handles errors gracefully (returns success=False, not raises)
4. fetch_fresh_wallets returns neutral/confidence 0 if no MCP tool available
5. fetch_funding_rate interprets rate per PROJECT.md rules (crowded longs/shorts)
</verification>

<success_criteria>
- [ ] nansen_mcp.py module created with 6 fetch functions
- [ ] Each function returns MCPSignalResult with structured data
- [ ] Error handling returns success=False instead of raising
- [ ] Fresh wallets handles unavailable MCP gracefully (NANS-10 pattern)
- [ ] Funding rate interprets per PROJECT.md contrarian rules
- [ ] All functions exported from agents/__init__.py
</success_criteria>

<output>
After completion, create `.planning/phases/15-nansen-agent/15-01-SUMMARY.md`
</output>
