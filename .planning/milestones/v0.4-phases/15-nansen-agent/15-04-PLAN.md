---
phase: 15-nansen-agent
plan: 04
type: execute
wave: 1
gap_closure: true
---

<objective>
Replace all MCP callback/invocation code with Nansen CLI subprocess calls.
Delete or replace nansen_mcp.py entirely. Build a production-ready agent that works without Claude Code.
</objective>

<tasks>

<task>
Run `nansen schema --pretty` in a subprocess to discover available commands, then implement:

1. A `run_nansen(args: list[str]) -> dict` helper using subprocess.run()
   - Reads NANSEN_API_KEY from environment
   - Returns parsed JSON data field
   - Raises NansenCLIError with error code on failure
   - Handles: CREDITS_EXHAUSTED, RATE_LIMITED, UNAUTHORIZED — log warning, return None

2. Five signal fetchers using CLI commands:
   - fetch_exchange_flows(symbol) → nansen smart-money netflow (exchange segment)
   - fetch_smart_money(symbol) → nansen smart-money netflow (smart money segment)
   - fetch_whale_activity(symbol) → nansen smart-money netflow (whale segment)
   - fetch_top_pnl(symbol) → nansen smart-money top-traders
   - fetch_fresh_wallets(symbol) → token screener or new wallet activity
   - fetch_funding_rate(symbol) → Hyperliquid perps endpoint

3. Use `nansen schema` output to get exact command names before writing any calls.

4. If a CLI command fails or returns no data: return neutral signal with confidence=0, log warning, continue.

5. Keep NansenSignal Pydantic model output and vault logging unchanged.

No MCP callbacks. No Claude dependency. subprocess only.
</task>

</tasks>
