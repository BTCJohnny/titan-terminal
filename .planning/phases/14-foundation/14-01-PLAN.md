---
phase: 14-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/models/nansen_signal.py
  - src/backend/models/telegram_signal.py
  - src/backend/models/__init__.py
autonomous: true
requirements:
  - MODL-01
  - MODL-02

must_haves:
  truths:
    - "NansenSignal model has all required fields (symbol, 5 signals, funding_rate, overall_signal, confidence, signal_count_bullish, signal_count_bearish, reasoning, timestamp)"
    - "TelegramSignal model has all required fields (symbol, signals_found, active_signals, overall_sentiment, confluence_count, avg_confidence, best_signal, reasoning, timestamp)"
    - "Both models validate correctly with Pydantic v2"
  artifacts:
    - path: "src/backend/models/nansen_signal.py"
      provides: "Extended NansenSignal model with MODL-01 fields"
      contains: "signal_count_bullish"
    - path: "src/backend/models/telegram_signal.py"
      provides: "Extended TelegramSignal model with MODL-02 fields"
      contains: "best_signal"
  key_links:
    - from: "src/backend/models/__init__.py"
      to: "NansenSignal, TelegramSignal"
      via: "exports in __all__"
      pattern: "__all__.*NansenSignal"
---

<objective>
Extend Pydantic models for Nansen and Telegram agents to match MODL-01 and MODL-02 requirements.

Purpose: Provide type-safe data structures for on-chain signal aggregation and Telegram signal confluence tracking.
Output: Updated NansenSignal and TelegramSignal models with all required fields for Phase 15/16 agents.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-foundation/14-CONTEXT.md

<interfaces>
<!-- Existing model patterns from codebase -->

From src/backend/models/nansen_signal.py (current structure):
```python
class NansenSignal(BaseModel):
    symbol: str = Field(...)
    exchange_flows: ExchangeFlows
    fresh_wallets: FreshWallets
    smart_money: SmartMoney
    top_pnl: TopPnL
    whale_activity: WhaleActivity
    overall_signal: OnChainOverall
```

From src/backend/models/telegram_signal.py (current structure):
```python
class TelegramSignal(BaseModel):
    symbol: str
    signals_found: int
    relevant_signals: list[TelegramChannelSignal]
    overall_sentiment: Literal["bullish", "bearish", "mixed", "neutral"]
    confluence_count: int
    confidence: int
    notes: str
```

From src/backend/config/settings.py:
```python
SIGNALS_DB_PATH: str = os.getenv("SIGNALS_DB_PATH", "data/signals.db")
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend NansenSignal model (MODL-01)</name>
  <files>src/backend/models/nansen_signal.py</files>
  <action>
Update NansenSignal model to include all MODL-01 required fields. Add to existing model:

1. `funding_rate: Optional[FundingRate]` - new nested model with:
   - `rate: Optional[float]` - Hyperliquid funding rate
   - `available: bool = True` - whether data was available from MCP
   - `interpretation: str` - meaning of rate for positioning

2. `signal_count_bullish: int = Field(..., ge=0, le=5)` - count of bullish signals (0-5)
3. `signal_count_bearish: int = Field(..., ge=0, le=5)` - count of bearish signals (0-5)
4. `reasoning: str = Field(...)` - explanation of overall signal derivation
5. `timestamp: datetime = Field(default_factory=datetime.utcnow)` - when signal was generated

Add the FundingRate nested model before NansenSignal class.

Update model_config example to include new fields.

Keep all existing fields (symbol, exchange_flows, fresh_wallets, smart_money, top_pnl, whale_activity, overall_signal) - this is an extension, not replacement.

Follow existing Pydantic v2 patterns in the file (BaseModel, Field, Literal).
  </action>
  <verify>
    <automated>cd /Users/johnny_main/Developer/projects/titan-terminal && python -c "from src.backend.models.nansen_signal import NansenSignal, FundingRate; print('NansenSignal fields:', [f for f in NansenSignal.model_fields.keys()]); assert 'signal_count_bullish' in NansenSignal.model_fields; assert 'funding_rate' in NansenSignal.model_fields; print('PASS')"</automated>
  </verify>
  <done>NansenSignal model has all MODL-01 fields: symbol, 5 signal types, funding_rate, overall_signal, signal_count_bullish, signal_count_bearish, reasoning, timestamp</done>
</task>

<task type="auto">
  <name>Task 2: Extend TelegramSignal model (MODL-02)</name>
  <files>src/backend/models/telegram_signal.py</files>
  <action>
Update TelegramSignal model to include all MODL-02 required fields. Add to existing model:

1. `active_signals: int = Field(..., ge=0)` - count of signals with status pending/active
2. `avg_confidence: float = Field(..., ge=0, le=100)` - average confidence across signals
3. `best_signal: Optional[TelegramChannelSignal] = None` - highest confidence signal
4. `reasoning: str = Field(...)` - explanation of sentiment derivation
5. `timestamp: datetime = Field(default_factory=datetime.utcnow)` - when aggregation was performed

Update existing fields:
- Keep `confidence` as `avg_confidence` (rename for clarity) OR add `avg_confidence` as new field (prefer adding new to avoid breaking changes)

Keep all existing fields (symbol, signals_found, relevant_signals, overall_sentiment, confluence_count, confidence, notes).

Follow existing Pydantic v2 patterns. Import datetime at top of file.
  </action>
  <verify>
    <automated>cd /Users/johnny_main/Developer/projects/titan-terminal && python -c "from src.backend.models.telegram_signal import TelegramSignal; print('TelegramSignal fields:', [f for f in TelegramSignal.model_fields.keys()]); assert 'active_signals' in TelegramSignal.model_fields; assert 'best_signal' in TelegramSignal.model_fields; assert 'reasoning' in TelegramSignal.model_fields; print('PASS')"</automated>
  </verify>
  <done>TelegramSignal model has all MODL-02 fields: symbol, signals_found, active_signals, overall_sentiment, confluence_count, avg_confidence, best_signal, reasoning, timestamp</done>
</task>

<task type="auto">
  <name>Task 3: Verify model exports</name>
  <files>src/backend/models/__init__.py</files>
  <action>
Verify models/__init__.py exports are correct. The file should already export NansenSignal and TelegramSignal.

If FundingRate was created as a public model, add it to exports:
1. Add `FundingRate` to imports from nansen_signal
2. Add `FundingRate` to __all__ list

Verify all existing exports remain (TASignal, TAMentorSignal, RiskOutput, etc.).
  </action>
  <verify>
    <automated>cd /Users/johnny_main/Developer/projects/titan-terminal && python -c "from src.backend.models import NansenSignal, TelegramSignal; print('Exports OK')"</automated>
  </verify>
  <done>Models module exports NansenSignal and TelegramSignal correctly</done>
</task>

</tasks>

<verification>
1. `python -c "from src.backend.models import NansenSignal, TelegramSignal; print('OK')"` - imports work
2. `python -c "from src.backend.models.nansen_signal import NansenSignal; print(NansenSignal.model_json_schema())"` - schema includes new fields
3. `python -c "from src.backend.models.telegram_signal import TelegramSignal; print(TelegramSignal.model_json_schema())"` - schema includes new fields
</verification>

<success_criteria>
- NansenSignal model validates with all MODL-01 fields
- TelegramSignal model validates with all MODL-02 fields
- Both models can be instantiated and serialized to JSON
- Existing model functionality preserved (backward compatible)
</success_criteria>

<output>
After completion, create `.planning/phases/14-foundation/14-01-SUMMARY.md`
</output>
